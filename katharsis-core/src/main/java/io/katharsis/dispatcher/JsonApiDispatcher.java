package io.katharsis.dispatcher;

import io.katharsis.dispatcher.handlers.JsonApiHandler;

/**
 * Dispatches JSON API requests to proper handlers.
 * (ieugen: seems to be identical to a handler so far)
 */
public interface JsonApiDispatcher extends JsonApiHandler {
}
